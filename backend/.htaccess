<IfModule mod_authz_core.c>
    Require all granted
</IfModule>

RewriteEngine On

# Route all requests to index.php (including OPTIONS for CORS preflight)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]

